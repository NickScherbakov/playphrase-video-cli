# 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА: Пустая страница GitHub Pages

## Диагностика

Страница по адресу https://nickscherbakov.github.io/playphrase-video-cli/ показывает черный экран.

### Возможные причины:

1. **GitHub Pages НЕ АКТИВИРОВАН** (наиболее вероятно)
2. Workflow не был запущен
3. Build не был выполнен
4. Нет коммитов после настройки

## ✅ РЕШЕНИЕ: Пошаговая активация

### Шаг 1: Проверьте настройки GitHub Pages

**ОБЯЗАТЕЛЬНО выполните:**

1. Откройте в браузере: https://github.com/NickScherbakov/playphrase-video-cli/settings/pages
2. Найдите секцию **"Build and deployment"**
3. В поле **"Source"** ОБЯЗАТЕЛЬНО выберите: **"GitHub Actions"**
   
   ⚠️ Если там стоит "Deploy from a branch" - это НЕПРАВИЛЬНО!
   ✅ Должно быть: "GitHub Actions"

4. Сохраните изменения

### Шаг 2: Запустите деплой

**В терминале VS Code выполните:**

```bash
# Дайте права на выполнение скрипта
chmod +x deploy.sh

# Запустите скрипт деплоя
./deploy.sh
```

**Или вручную:**

```bash
# Добавить все файлы
git add .

# Создать коммит
git commit -m "Setup GitHub Pages deployment"

# Отправить на GitHub
git push origin main
```

### Шаг 3: Проверьте GitHub Actions

1. Откройте: https://github.com/NickScherbakov/playphrase-video-cli/actions
2. Вы должны увидеть запущенный workflow "Deploy to GitHub Pages"
3. Дождитесь зеленой галочки ✅ (1-2 минуты)
4. Оба job'а должны завершиться успешно:
   - ✅ build
   - ✅ deploy

### Шаг 4: Проверьте деплой

Через 1-2 минуты после успешного workflow:

1. Откройте: https://nickscherbakov.github.io/playphrase-video-cli/
2. Нажмите Ctrl+Shift+R (Windows) или Cmd+Shift+R (Mac) для жесткого обновления
3. Приложение должно загрузиться

## 🔍 Детальная диагностика

### Проверка 1: Настройки репозитория

```bash
# Проверьте удаленный репозиторий
git remote -v
```

Должно быть:
```
origin  https://github.com/NickScherbakov/playphrase-video-cli (fetch)
origin  https://github.com/NickScherbakov/playphrase-video-cli (push)
```

### Проверка 2: Текущая ветка

```bash
# Проверьте текущую ветку
git branch --show-current
```

Должно быть: `main`

### Проверка 3: Статус изменений

```bash
# Проверьте статус
git status
```

### Проверка 4: Последний коммит

```bash
# Посмотрите последние коммиты
git log --oneline -5
```

### Проверка 5: Workflow файл существует

```bash
# Проверьте наличие workflow
ls -la .github/workflows/deploy.yml
```

Файл должен существовать.

### Проверка 6: Vite конфигурация

```bash
# Проверьте base path в vite.config.ts
grep "base:" vite.config.ts
```

Должно быть: `base: '/playphrase-video-cli/',`

## 🚨 Если проблема сохраняется

### Вариант А: Ручной запуск workflow

1. Откройте: https://github.com/NickScherbakov/playphrase-video-cli/actions
2. Слева выберите "Deploy to GitHub Pages"
3. Справа нажмите "Run workflow"
4. Выберите branch: `main`
5. Нажмите зеленую кнопку "Run workflow"

### Вариант Б: Проверка прав GitHub Actions

1. Откройте: https://github.com/NickScherbakov/playphrase-video-cli/settings/actions
2. Секция "Workflow permissions"
3. Должно быть выбрано: **"Read and write permissions"**
4. Галочка: **"Allow GitHub Actions to create and approve pull requests"**

### Вариант В: Пересоздание GitHub Pages

1. Settings → Pages
2. Если там что-то есть, выберите "None" и сохраните
3. Подождите 10 секунд
4. Снова выберите "GitHub Actions" и сохраните

## 📞 Контрольный чеклист

Перед тем как обратиться за помощью, проверьте:

- [ ] GitHub Pages Source = "GitHub Actions" (не "Deploy from a branch")
- [ ] Сделан git push в ветку main
- [ ] GitHub Actions workflow запустился и завершился успешно
- [ ] Прошло больше 2 минут после успешного workflow
- [ ] Сделан жесткий refresh страницы (Ctrl+Shift+R)
- [ ] Проверена консоль браузера (F12) на ошибки
- [ ] Workflow permissions = "Read and write"

## 💡 Быстрый тест

Откройте в браузере и проверьте:

**Шаг 1:** https://github.com/NickScherbakov/playphrase-video-cli/actions
- Есть ли запущенные или завершенные workflows?

**Шаг 2:** https://github.com/NickScherbakov/playphrase-video-cli/settings/pages
- Source = "GitHub Actions"?

**Шаг 3:** https://nickscherbakov.github.io/playphrase-video-cli/
- Откройте DevTools (F12) → Console
- Какие ошибки показывает?

## 🎯 Ожидаемый результат

После правильной настройки:
- ✅ Страница загружается
- ✅ Видно поле поиска
- ✅ Можно искать фразы типа "I'll be back"
- ✅ Воспроизводятся видео клипы

---

**Важно:** Без активации GitHub Pages в настройках репозитория сайт НИКОГДА не заработает, даже если все файлы настроены правильно!
