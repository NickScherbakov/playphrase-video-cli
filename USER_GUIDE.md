# Руководство пользователя PlayPhrase MVP

## Что это за приложение?

**PlayPhrase MVP** — это веб-приложение для мгновенного поиска и воспроизведения фрагментов видео по текстовым фразам из субтитров. Вдохновлено сервисом [playphrase.me](https://playphrase.me).

### Практическая польза

1. **Для изучающих язык**: Найдите, как носители произносят конкретные слова или фразы в контексте реальных фильмов и сериалов
2. **Для создателей контента**: Быстро находите нужные цитаты из видеоархива для монтажа или референсов
3. **Для исследователей**: Анализируйте использование определенных выражений в медиаконтенте
4. **Для развлечения**: Находите культовые реплики из любимых фильмов одним поиском

---

## Описание интерфейса

### Главная страница (Landing Page)

При открытии `http://127.0.0.1:8000/` вы видите:

```
┌─────────────────────────────────────────────────────────┐
│                    PlayPhrase MVP                       │
├─────────────────────────────────────────────────────────┤
│ [Поисковая строка          ] [Search] [Prev] [Next]    │
│                               [Reindex]                 │
├─────────────────────────────────────────────────────────┤
│ Upload Video + Subtitle (optional pair)                │
│ [Choose File] demo.mp4  [Choose File] demo.srt [Upload]│
│ Status: ...                                             │
├─────────────────────────────────────────────────────────┤
│ ┌───────────────────────────────────────┐              │
│ │                                       │              │
│ │         ВИДЕОПЛЕЕР                    │              │
│ │         640 x 360                     │              │
│ │                                       │              │
│ └───────────────────────────────────────┘              │
│ [▶️ Воспроизвести]                                      │
│ Текущая фраза: ...                                      │
│ Video ID: 2 | Clip: 4.0s - 6.0s                        │
├─────────────────────────────────────────────────────────┤
│ РЕЗУЛЬТАТЫ ПОИСКА:                                      │
│ #1 [1.00s] Hello world                                 │
│ #2 [4.00s] I will be back                              │
│ #3 [7.00s] Force is with you                           │
│ #4 [9.50s] Show me the money                           │
└─────────────────────────────────────────────────────────┘
```

---

## Элементы управления

### 1. Блок поиска (верхняя панель)

#### 🔍 Поисковая строка
- **Назначение**: Ввод текста для поиска в субтитрах
- **Как использовать**: Введите слово или фразу (например: "back", "hello", "money")
- **Особенности**: 
  - Поиск нечувствителен к регистру
  - Поддерживает частичное совпадение
  - Можно искать как отдельные слова, так и целые предложения

#### 🔘 Кнопка "Search"
- **Назначение**: Запуск поиска по введенному запросу
- **Альтернатива**: Нажмите `Enter` в поисковой строке
- **Результат**: Список совпадений появится внизу страницы

#### ⬅️ Кнопка "Prev"
- **Назначение**: Переключение на предыдущий результат в списке
- **Когда активна**: После выполнения поиска с несколькими результатами
- **Эффект**: Автоматически загружает и позиционирует видео на новый фрагмент

#### ➡️ Кнопка "Next"
- **Назначение**: Переключение на следующий результат
- **Навигация**: Циклически перебирает все найденные фрагменты
- **Удобство**: Быстрый просмотр всех вхождений фразы

#### 🔄 Кнопка "Reindex"
- **Назначение**: Перестроить базу данных субтитров
- **Когда использовать**: 
  - После добавления новых видео/субтитров в папку `media/`
  - При повреждении базы данных
  - Для обновления индекса полнотекстового поиска
- **Внимание**: Процесс может занять время при большом объеме данных

---

### 2. Блок загрузки файлов

#### 📁 "Choose File" (Видео)
- **Назначение**: Загрузка видеофайла
- **Поддерживаемые форматы**: `.mp4`, `.mkv`
- **Рекомендации**: Убедитесь, что есть соответствующий файл субтитров

#### 📄 "Choose File" (Субтитры)
- **Назначение**: Загрузка файла субтитров
- **Формат**: `.srt` (SubRip)
- **Автоматика**: Если имя совпадает с видео, они связываются автоматически

#### ⬆️ Кнопка "Upload"
- **Назначение**: Отправка выбранных файлов на сервер
- **Процесс**: 
  1. Файлы сохраняются в папку `media/`
  2. Субтитры парсятся и добавляются в базу данных
  3. Статус показывает количество добавленных записей
- **Статус**: Отображает результат: `"Saved. Inserted 42 entries."`

---

### 3. Видеоплеер

#### 🎬 Плеер HTML5
- **Размер**: 640×360 пикселей
- **Функции**: Стандартные контролы браузера (пауза, громкость, полноэкранный режим)
- **Особенности**: 
  - Автоматическая перемотка на начало найденного фрагмента
  - Авто-стоп в конце интервала субтитров
  - Поддержка перемотки вручную

#### ▶️ Кнопка "Воспроизвести"
- **Назначение**: Ручной запуск воспроизведения
- **Когда нужна**: 
  - Если браузер блокирует автовоспроизведение
  - Для повторного просмотра фрагмента
  - После паузы
- **Цвет**: Зеленый (#28a745), хорошо заметна

#### 📝 Информационный блок
- **"Текущая фраза"**: Отображает текст из субтитров
- **"Video ID"**: Идентификатор записи в базе данных
- **"Clip"**: Временной интервал фрагмента (например: `4.0s - 6.0s`)
- **Ошибки**: Если есть проблемы с загрузкой, появится сообщение об ошибке

---

### 4. Список результатов поиска

#### 📋 Панель результатов
- **Расположение**: Под видеоплеером
- **Прокрутка**: Максимальная высота 300px, вертикальная прокрутка
- **Подсветка**: Активный результат выделен серым фоном

#### Формат записи:
```
#[ID] [время_начала] текст_фразы
```
Пример: `#2 [4.00s] I will be back`

#### Интерактивность
- **Клик на результат**: Мгновенно переключает видео на этот фрагмент
- **Автоматическая подсветка**: Текущий активный фрагмент всегда выделен

---

## Пошаговая инструкция использования

### Сценарий 1: Базовый поиск (Quick Start)

1. **Откройте приложение**: Перейдите в браузере на `http://127.0.0.1:8000/`
2. **Введите запрос**: Напечатайте слово в поисковой строке, например: `back`
3. **Нажмите "Search"** или клавишу `Enter`
4. **Дождитесь загрузки**: Видео автоматически перемотается на нужный момент
5. **Нажмите зеленую кнопку** "▶️ Воспроизвести" (если видео не запустилось само)
6. **Просмотрите фрагмент**: Воспроизведение автоматически остановится в конце фразы

### Сценарий 2: Навигация по результатам

1. Выполните поиск (например: `world`)
2. Посмотрите первый результат
3. Нажмите **"Next"**, чтобы перейти к следующему вхождению
4. Используйте **"Prev"**, чтобы вернуться назад
5. **Альтернатива**: Кликните на любой результат в списке снизу

### Сценарий 3: Загрузка своих видео

1. **Подготовьте файлы**:
   - Видео: `my_movie.mp4`
   - Субтитры: `my_movie.srt` (важно: одинаковое имя файла!)

2. **Загрузите на сервер**:
   - Нажмите первую кнопку "Choose File" → выберите видео
   - Нажмите вторую "Choose File" → выберите субтитры
   - Нажмите **"Upload"**

3. **Дождитесь подтверждения**: 
   - Статус покажет: `"Saved. Inserted 156 entries."`

4. **Начните поиск**: Теперь можно искать фразы из нового видео

### Сценарий 4: Обновление базы данных

1. Если вы вручную добавили файлы в папку `media/` (минуя веб-интерфейс)
2. Нажмите кнопку **"Reindex"**
3. Подтвердите действие в диалоге
4. Дождитесь сообщения: `"Reindex complete: 42 rows"`
5. База данных обновлена, можно искать новый контент

---

## Технические особенности

### Поисковая система
- **FTS5**: Использует полнотекстовый поиск SQLite
- **Скорость**: Мгновенный поиск даже в больших коллекциях
- **Fallback**: Если FTS не находит, используется `LIKE` для частичного совпадения

### Воспроизведение видео
- **Технология**: HTML5 `<video>` + прямой доступ к файлам через `/media/`
- **Seeking**: Точная перемотка на миллисекунды начала фразы
- **Авто-стоп**: JavaScript следит за `timeupdate` и останавливает в конце интервала
- **Формат**: Воспроизводятся оригинальные файлы без перекодировки

### Субтитры
- **Формат**: SubRip (`.srt`)
- **Парсинг**: Regex-based парсер с поддержкой HTML-тегов в субтитрах
- **Точность**: Миллисекундная точность временных меток

---

## Решение проблем

### Видео не воспроизводится

**Проблема**: После поиска плеер показывает ошибку "no supported sources"

**Решение**:
1. Проверьте, что файл `media/demo.mp4` существует
2. Откройте DevTools (F12) → вкладка Network → найдите запрос к видеофайлу
3. Убедитесь, что Content-Type: `video/mp4`
4. Перезапустите сервер: `python run_app.py`

### Поиск ничего не находит

**Проблема**: Запрос возвращает пустой список

**Решение**:
1. Нажмите **"Reindex"** для обновления базы данных
2. Проверьте, что в папке `media/` есть `.srt` файлы
3. Попробуйте более короткий запрос (одно слово вместо фразы)

### Не загружаются файлы

**Проблема**: После Upload ничего не происходит

**Решение**:
1. Проверьте формат: видео должно быть `.mp4` или `.mkv`, субтитры — `.srt`
2. Убедитесь, что сервер запущен (терминал должен показывать логи)
3. Проверьте размер файла (очень большие файлы могут загружаться долго)

---

## Примеры использования

### Для изучения английского

**Цель**: Услышать, как носители произносят слово "actually"

1. Поиск: `actually`
2. Результат: 15 фрагментов из разных фильмов
3. Навигация: Используйте Next/Prev для прослушивания разных интонаций
4. **Польза**: Развитие навыка восприятия на слух в реальном контексте

### Для создания видеомонтажа

**Цель**: Найти сцену с фразой "I'll be back" для клипа

1. Поиск: `I'll be back`
2. Результат: Фрагмент с точным таймингом `4.0s - 6.0s`
3. **Использование**: Откройте видео в редакторе, вырежьте нужный интервал
4. **Экономия времени**: Вместо просмотра всего фильма — точная навигация

### Для лингвистического анализа

**Цель**: Исследовать употребление фразы "show me the money"

1. Поиск: `show me the money`
2. Анализ: Контекст употребления, эмоциональная окраска
3. **Применение**: Статья о влиянии культовых фраз на популярную культуру

---

## Архитектура приложения

### Структура проекта

```
playphrase/
├── app.py                   # FastAPI сервер (backend)
├── index.html               # Веб-интерфейс (frontend)
├── build_subtitles_db.py    # Парсер субтитров + индексация
├── run_app.py               # Launcher (один файл для запуска)
├── requirements.txt         # Python-зависимости
├── media/                   # Папка с видео и субтитрами
│   ├── demo.mp4
│   └── demo.srt
├── subtitles.db             # SQLite база данных
└── .venv/                   # Виртуальное окружение Python
```

### Стек технологий

**Backend**:
- Python 3.14
- FastAPI (веб-фреймворк)
- SQLite + FTS5 (полнотекстовый поиск)
- FFmpeg (видеообработка, не используется в текущей версии)

**Frontend**:
- Vanilla JavaScript (без фреймворков)
- HTML5 Video API
- Fetch API для AJAX-запросов

**База данных**:
- Таблица `subtitles`: id, text, start_time, end_time, video_path
- Виртуальная таблица `subtitles_fts`: FTS5-индекс для быстрого поиска

---

## API документация (для разработчиков)

### GET /search
Поиск фраз в субтитрах

**Параметры**:
- `q` (string, required): Поисковый запрос
- `limit` (int, optional): Максимум результатов (по умолчанию 25)

**Ответ**:
```json
[
  {
    "id": 2,
    "text": "I will be back",
    "start_time": 4.0,
    "end_time": 6.0,
    "video_path": "C:\\...\\media\\demo.mp4"
  }
]
```

### GET /stream/{subtitle_id}
**Deprecated**: Streaming-эндпоинт для FFmpeg-нарезки

**Параметры**:
- `subtitle_id` (int): ID записи в базе
- `reencode` (bool, optional): Принудительное перекодирование

**Ответ**: `video/mp4` stream

### GET /media/{filename}
Статическая отдача видеофайлов (используется текущей версией)

**Пример**: `/media/demo.mp4`

### POST /upload
Загрузка видео и/или субтитров

**Тело запроса** (multipart/form-data):
- `video_file` (file, optional): `.mp4` или `.mkv`
- `subtitle_file` (file, optional): `.srt`

**Ответ**:
```json
{
  "video_saved": "media/my_movie.mp4",
  "subtitle_saved": "media/my_movie.srt",
  "entries_inserted": 156
}
```

### POST /reindex
Полная перестройка базы данных

**Ответ**:
```json
{
  "rows": 42,
  "message": "Reindex complete"
}
```

---

## Расширения и улучшения (Roadmap)

### Запланированные функции

1. **Многоязычность**: Поддержка субтитров на разных языках
2. **Фильтры**: По названию видео, году, жанру
3. **Плейлисты**: Сохранение избранных фрагментов
4. **Экспорт**: Скачивание нарезанных клипов
5. **Пакетная загрузка**: Drag & drop нескольких файлов
6. **Предпросмотр**: Thumbnails для результатов поиска

### Для продакшена

- Docker-контейнеризация
- Reverse proxy (Nginx)
- HTTPS сертификаты
- Кэширование медиафайлов
- CDN для статики
- Аутентификация пользователей

---

## Лицензия и вклад

Проект создан как MVP (Minimum Viable Product) для демонстрации концепции.

**Используемые технологии** — open source с разрешительными лицензиями.

---

## Контакты и поддержка

При возникновении вопросов:
1. Проверьте раздел "Решение проблем"
2. Изучите логи сервера в терминале
3. Откройте Browser DevTools (F12) для диагностики frontend-ошибок

**Версия**: MVP 0.1.0  
**Дата обновления**: Ноябрь 2025
