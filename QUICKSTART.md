# Быстрый старт PlayPhrase MVP

## Текущее состояние
✅ Все файлы созданы
✅ Виртуальное окружение настроено
✅ Зависимости установлены
✅ Подготовлены демо‑субтитры `media/demo.srt`

## Что у вас есть

### Файлы проекта
- `app.py` — FastAPI backend с эндпоинтами `/`, `/search`, `/upload`, `/reindex`, `/media/...` (и устаревшим `/stream/{id}`).
- `build_subtitles_db.py` — парсер SRT файлов и построитель индекса FTS5.
- `run_app.py` — скрипт «всё в одном»: создаёт демо‑данные (при необходимости), строит базу и запускает сервер.
- `init_empty_db.py` — создание пустой БД для экспериментов (опционально).
- `index.html` — веб‑интерфейс с поиском, загрузкой файлов и видеоплеером.
- `requirements.txt` — FastAPI, Uvicorn, Pydantic v2 и др.
- `subtitles.db` — SQLite база с индексом FTS5 (создаётся автоматически).

### Python окружение
- Виртуальное окружение: `.venv`
- Python: 3.14

## Как запустить

### Вариант 1 (рекомендуется): один скрипт

```powershell
# Из корня проекта
python run_app.py
```

Скрипт сам:
- создаст папку `media/` (если её нет);
- создаст пример `media/demo.srt`;
- построит/обновит `subtitles.db`;
- запустит сервер на `http://127.0.0.1:8000/`.

Затем откройте в браузере:

```
http://127.0.0.1:8000
```

На главной странице вы можете сразу:
- ввести фразу в поле поиска и нажать **Search**;
- листать результаты кнопками **Prev/Next** или кликом по списку;
- нажать зелёную кнопку **«Воспроизвести»**, если браузер не запустил видео сам;
- загрузить свои файлы через блок **Upload Video + Subtitle**;
- нажать **Reindex** для полной перестройки базы (если что‑то добавляли вручную в `media/`).

### Вариант 2: ручной запуск сервера

Если хотите сами управлять базой и индексом:

1. Добавьте медиафайлы (опционально)

```powershell
mkdir media

# Примеры пар файлов:
# media/movie1.mp4 + media/movie1.srt
# media/movie2.mkv + media/movie2.srt

python build_subtitles_db.py ./media --db subtitles.db
```

2. Запустите сервер через Uvicorn

```powershell
.\.venv\Scripts\python.exe -m uvicorn app:app --host 127.0.0.1 --port 8000
```

После этого используйте веб‑интерфейс так же, как в варианте 1.

## Минимальная проверка через PowerShell

```powershell
# Проверка главной страницы
Invoke-WebRequest http://127.0.0.1:8000/

# Поиск фразы
Invoke-WebRequest http://127.0.0.1:8000/search?q=world
```

## Рекомендации

1. **ffmpeg**: полезен, если хотите экспериментировать со старым эндпоинтом `/stream/{id}` (`ffmpeg -version`).
2. **Структура SRT**: файлы должны быть в стандартном формате с таймкодами.
3. **Производительность**: для большой коллекции (~1000+ фильмов) держите файлы в `media/` и периодически нажимайте **Reindex**.
4. **Кодировка**: парсер использует UTF‑8 с `errors='replace'`, поэтому большинство субтитров будет обработано корректно.

## Возможные улучшения (не входят в MVP)

- Кэширование клипов
- Пагинация результатов поиска
- Поддержка нескольких языков субтитров
- Поиск по регулярным выражениям
- Web UI с React/Vue вместо vanilla JS
- Docker‑контейнер для развёртывания

---

**Проект готов к использованию!** 🎉

Самый простой способ запуска:
```powershell
python run_app.py
```
